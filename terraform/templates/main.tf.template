terraform {
  required_providers {
    hcloud = {
      source = "hetznercloud/hcloud"
    }
  }
}

module "hetzner" {
  source       = "../../platforms/hetzner"
  name_prefix  = "{{ENV_NAME}}-mc"
  ssh_keys     = {{SSH_KEYS}}
  
  servers = {{SERVERS_CONFIG}}
}